import 'package:flutter/material.dart';

class HomeInformation extends StatelessWidget {
  const HomeInformation({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("HomeIn formation"),
    );
  }
}
